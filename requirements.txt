# Core
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-dotenv==1.0.0

# Database
sqlalchemy==2.0.25
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.1

# Authentication & Security
# Removidos: python-jose (CVE, JWT nunca usado), passlib[bcrypt] (sem hash de
# senha) e python-multipart (sem endpoints de form) — eram código morto.

# HTTP & Async
httpx>=0.28.1  # Compatível com google-genai
# aiohttp>=3.10: o SDK google-genai usa aiohttp.ClientConnectorDNSError no
# tratamento de erro do cliente async (atributo adicionado só no 3.10). No 3.9.1
# isso estoura AttributeError, mascara o erro real e mata o retry do SDK —
# quebrando ContextualAnalyzer e geração de imagem via Gemini.
aiohttp==3.10.11

# Data Validation
pydantic==2.5.3
pydantic-settings==2.1.0
email-validator==2.1.0

# Automation & Scheduling
# Removido apscheduler: nunca importado (agendamento é via cron externo).
feedparser==6.0.11

# AI & ML
# Removidos torch/sentence-transformers/huggingface-hub e numpy/scikit-learn:
# o engine de embeddings (~500MB) não é usado (dedup padrão = tfidf, impl.
# própria) e scikit/numpy não são importados em lugar nenhum.
openai>=1.10.0  # Compatível com httpx>=0.28.1
google-genai>=1.5.0  # Google Gemini SDK
anthropic>=0.18.0  # Claude API SDK

# Image Processing
cloudinary==1.38.0
Pillow==10.2.0

# Logging & Monitoring
loguru==0.7.2
sentry-sdk[fastapi]==1.39.2

# Redis (for rate limiting and caching)
redis==5.0.1

# Rate Limiting
slowapi==0.1.9

# Utils
python-slugify==8.0.1
markdown==3.5.1
beautifulsoup4>=4.12.0
json-repair>=0.44.0  # Conserta JSON malformado de LLMs (aspas/newlines não-escapadas)

# Social Media
tweepy>=4.14.0

# Web Search
ddgs>=7.0.0

# Testing
pytest==8.0.0
pytest-asyncio==0.23.3
pytest-cov==4.1.0
aiosqlite==0.19.0
pytest-mock==3.12.0
